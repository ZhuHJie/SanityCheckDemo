using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace ReNameStoredProcedure
{
    class Program
    {
        static void Main(string[] args)
        {
            Test.Show();
            //Console.WriteLine("Start");

            //string path = @"D:\Test";
            //DirectoryInfo root = new(path);
            //int index = 0;
            //foreach (FileInfo f in root.GetFiles())
            //{
            //    index++;
            //    var savePath = @$"{path}\{index.ToString().PadLeft(2, '0')}_GMR_{f.Name.Replace(".StoredProcedure.", ".")}";
            //    File.Move(f.FullName, savePath);
            //}

            //Console.WriteLine("Success");
        }


    }
}
